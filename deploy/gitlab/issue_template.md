<!-- .gitlab/issue_templates/Playground Account.md
     For teams that want requests to start as an issue. Fill this in, then an
     approver runs the `request` and `approve` pipeline jobs, or acts in the
     web UI. The issue is the conversation; the queue is in AWS. -->

## Playground account request

| | |
|---|---|
| Name | `dev-yourname` (lowercase, digits, dashes) |
| Owner | your Identity Center email |
| Lifetime | 14d |
| Monthly budget | $50 |

**Purpose**

One or two sentences on what you will try.

/label ~"playground-request"
